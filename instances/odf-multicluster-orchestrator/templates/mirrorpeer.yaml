apiVersion: multicluster.odf.openshift.io/v1alpha1
kind: MirrorPeer
metadata:
  name: {{ .Values.name }}
spec:
  items:
  {{- range $idx, $cluster := .Values.peers -}}
  {{- include "mirror.clusterRef" $cluster | nindent 2 -}}
  {{- end -}}
